import com.robin.camarasa.nutritivecoach.model.*;

rule "R1"
    when
        Preferences(value > 3f, value < 7f)
    then
        System.out.println("\nAvis mitigé\n");
end

rule "R2"
    when
        Preferences(value <= 3f)
    then
        insert(new String("Bad mark"));
end

rule "R3"
    when
        Rate(value >= 7f)
    then
        System.out.println("\nSuper avis\n");
end
