import com.robin.camarasa.nutritivecoach.model.*;
import com.robin.camarasa.nutritivecoach.Expert_system.*;

rule "R1"
    when
        Preferences(value > 0.5)
    then
          System.out.println("Test");
        //delete(Recipe (getId() == $p.getRecipe().getId()));
end
